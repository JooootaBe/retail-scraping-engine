# Runtime externo mínimo para extractor_makro_v13.py y herramientas auxiliares.
# Compatible con Python 3.12.
#
# Después de instalar estas dependencias, descargar el navegador gestionado:
#   python -m playwright install chromium
#
# El canal predeterminado del proyecto es "chrome". Google Chrome, si se desea
# usar ese canal, es una dependencia del sistema; el motor cae a Chromium
# gestionado por Playwright cuando Chrome no está disponible.
playwright==1.62.0
